#!/usr/bin/env node

'use strict';

const { strapi_app } = require('@pfapi/utils');
const path = require('path');

(async() => {
    process.chdir(path.dirname(__dirname));
    await strapi_app.stop(true);
})();