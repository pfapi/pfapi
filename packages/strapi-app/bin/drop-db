#!/usr/bin/env node

'use strict';

const { drop_db } = require('@pfapi/test-helpers');

(async () => {
    let client = 'sqlite';
    if (process.argv.length === 3) {
        client = process.argv[2];
    }
    await drop_db(client);
})();
