#!/usr/bin/env node

'use strict';

const path = require('path');
process.chdir(path.dirname(__dirname));

// use nginx, mysql and redis cluster
//
require('dotenv').config();
const { use_plus_env } = require('@pfapi/test-helpers');
use_plus_env();